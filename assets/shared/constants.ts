/**
 * @fileoverview Constants shared by PDF viewers (PdfSignable, PdfTemplate).
 */
if (typeof console !== 'undefined') console.debug('[PdfSignable] loaded: shared/constants.ts');
/**
 * Constants shared by signable and acroform viewers.
 */
if (typeof console !== 'undefined') console.debug('[PdfSignable] loaded: shared/constants.ts');
/** Gutter (px) reserved from container size for fit-width/fit-page scale. */
export const SCALE_GUTTER = 24;
