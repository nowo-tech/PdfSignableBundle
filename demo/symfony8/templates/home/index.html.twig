{% extends 'base.html.twig' %}

{% block title %}Home - PdfSignable Demo{% endblock %}

{% block body %}
<div class="card mb-4">
    <div class="card-body">
        <h5 class="card-title">PdfSignable Bundle Demo (Symfony 8)</h5>
        <p class="card-text">Demos are split into <strong>Define signature areas</strong> (where boxes go, validation, layout) and <strong>Signing</strong> (draw/upload signature in box, legal disclaimer). Each illustrates a way to configure <code>SignatureCoordinatesType</code>.</p>
    </div>
</div>
<div class="row g-3">
{% for section in demos_home_sections() %}
    <div class="col-12"><h5 class="border-bottom pb-2 {{ loop.first ? '' : 'mt-4' }}">{{ section.title }}</h5></div>
    {% for item in section.items %}
        {% if item.type == 'subsection' %}
    <div class="col-12 {{ loop.first ? '' : 'mt-2' }}"><h6 class="text-muted">{{ item.title|raw }}</h6></div>
        {% elseif item.type == 'cards' %}
            {% for card in item.cards %}
    <div class="{{ item.col_class|default('col-md-6 col-lg-4') }}">
        <div class="card h-100 {{ card.card_class|default('') }}">
            <div class="card-body">
                <h6 class="card-title">{{ card.title|raw }}</h6>
                <ul class="card-text small mb-0">
                    {% for bullet in card.bullets %}
                    <li>{{ bullet|raw }}</li>
                    {% endfor %}
                </ul>
                <a href="{{ path(card.route) }}" class="btn btn-sm {{ card.btn_class }} mt-2">Open</a>
            </div>
        </div>
    </div>
            {% endfor %}
        {% endif %}
    {% endfor %}
{% endfor %}
</div>
{% endblock %}



